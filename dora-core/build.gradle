apply plugin: 'java'
apply from: '../java_jacoco.gradle'

configurations {
    provided
    compile.extendsFrom provided
}

dependencies {
    provided deps.android
    testCompile deps.junit
    testCompile deps.truth
}
